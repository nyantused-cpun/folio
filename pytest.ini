[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = src
addopts = -v --tb=short -p no:cacheprovider -m "not internal"
markers =
    internal: requires internal workspace hooks/fixtures/data not shipped in the public release
