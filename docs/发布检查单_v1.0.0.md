# Folio v1.0.0 发布检查单（用户终端执行）

> 2026-08-15 生成。AI 侧全部发布前置已完成（本地 commit ×2 + tag v1.0.0、插件层装配 PASS、脱敏 0 残留、文档面齐备）；以下 3 步需要你的 GitHub 账号与终端。

## ① GitHub 建仓 + push（10 分钟）

```powershell
# 1. github.com 新建空仓库：名为 folio（不要勾选 README/.gitignore/LICENSE——仓库里都有）

# 2. 关联并推送（本地已 commit 并打 tag v1.0.0，188 个文件）
cd D:\knowledge base\folio
git remote add origin https://github.com/<你的用户名>/folio.git
git push -u origin master --tags

# 3. 仓库设置里加 topic：dsh-plugin（Settings → Topics）
```

推送后验证：GitHub 页面能看到 README 渲染 + v1.0.0 release 候选（tag 已就位，可点 tag 创建 Release，正文用 `RELEASE_NOTES.md`）。

## ② pytest 双关（30-60 分钟）

```powershell
# folio 发布仓库（AI 被守卫机械拦截非 CLI python，属设计行为，需你跑）
cd D:\knowledge base\folio
..\.venv\Scripts\python.exe -m pytest tests -q

# 主工作区全量（改名波及 test_inbox_scan 等）
cd D:\knowledge base
.venv\Scripts\python.exe -m pytest tests -q
```

- 预期全绿（1145+ 用例）；若红灯，把报错贴回会话，我来修；
- 另跑一次 `.\setup\install.ps1`（五步自检 + 能力等级输出）。

## ③ 重启 dsh web + 插件运行级验证（10 分钟）

重启后**开一个新会话**，按顺序验证：

| # | 验证 | 预期 |
|---|---|---|
| 1 | 第一条消息后 | 收到【兰亭入口协议】提醒（folio-events 生效，已实证过一次） |
| 2 | 问「你有哪些 folio_ 工具」或直接说「用 folio_status 看下客户状态」 | 15 个工具可见可调，返回客户状态 JSON |
| 3 | 让 AI 跑 `Set-Content "output/xxx/测试.html" -Value x` | 被 presales-guard 拒绝，原因含「禁止 shell 直写」 |
| 4 | 让 AI 直接 `python -c "print(1)"` | 被拒（guard 老功能复测） |
| 5 | 关闭该会话 | 宿主日志出现 `[folio-events] auto-save` 行（自动存档） |

## ④ 发布后（可选，30 分钟）

- **Repo description**（仓库页 About 栏，≤350 字符，直接粘贴）：

  ```
  Folio (兰亭): consulting document-generation engine with 5-stage pipeline (intake → memory → methodology → deliverable → proof). Native DSH plugin stack: 15 tools, session-protocol events, L0 guard, agent preset. Swappable methodology packs, zero-key start.
  ```

- **GitHub Release v1.0.0**（点 tag `v1.0.0` → Create release）：
  - 标题：`v1.0.0 — Folio (兰亭) first release: consulting doc engine, natively on DSH`
  - 正文：先粘贴下面这段英文摘要，再贴 `RELEASE_NOTES.md` 全文（中文）：

  ```markdown
  **Folio（兰亭）— a document-generation engine for consultants, built natively for DSH.**

  Folio turns client materials into deliverable consulting documents through a five-stage pipeline: intake (archive, parse, chunk, index), chronicle (per-client memory with decision records and knowledge graphs), craft (swappable methodology packs), press (HTML / PPTX / DOCX / quote generated from one source of truth), and proof (mechanical gates, independent review, citation audit).

  It ships as a native DeepSeek Harness plugin stack: 15 first-party tools (`folio_*`) with schema-validated parameters, a session-protocol event plugin (auto session-start reminders, auto-save on session close), an L0 guard plugin that blocks direct writes to deliverable paths and non-CLI Python calls, and a custom agent preset ("兰亭 mode") with a swappable persona. The engine kernel stays host-agnostic — the same CLI works in Trae and Kimi — while the DSH layer is pure official extension points, zero forks.

  Methodology is replaceable without touching code: Folio Packs (light / standard / deep) are YAML + SKILL.md, so switching domains means swapping a pack, not rewriting the engine. Zero API keys to start under DSH — reading images, web search, and independent review ride the host's native capabilities (`LLM_MODE=host`).
  ```

- 提交 awesome 收录 PR：
  - [awesome-dsh-plugin](https://github.com/awesome-dsh-plugin/awesome-dsh-plugin)（PR 描述里贴 `RELEASE_NOTES.md` 底部「Awesome 列表收录描述」段）；
  - [awesome-deepseek-harness](https://github.com/0xsline/awesome-deepseek-harness)（同上）。

## 已知事项

- `settings.yaml` 默认模型当前为 deepseek-v4-flash（若想主脑用 v4-pro，在 Web 模型选择器切回即可，设置自动落盘）；
- `compact 通用` 已由 AI 跑完（context 超长已清）；
- v1.1 路线（domain-pack 契约等）见 CHANGELOG 与交接文档 §5.3。
